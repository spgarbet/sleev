# Generated by using Rcpp::compileAttributes() -> do not edit by hand
# Generator token: 10BE3573-1514-4C36-9D1C-5A225CD40393

WaldCoxphGeneralSplineProfile <- function(pB, p_col_sum, ZW_theta, X_uni_theta, e_X_uni_theta, e_X_theta, lambda, lambda0, Lambda, q_row_sum, p, p0, P_theta, q, logp, theta, Y, Delta, X, Bspline_uni, ZW, X_uni, X_uni_ind, Bspline_uni_ind, Y_uni_event, Y_uni_event_n, Y_risk_ind, p_static, n, n2, m, n_event_uni, s, n_minus_n2, X_nc, ZW_nc, MAX_ITER, TOL) {
    .Call(`_CombinedReg_WaldCoxphGeneralSplineProfile`, pB, p_col_sum, ZW_theta, X_uni_theta, e_X_uni_theta, e_X_theta, lambda, lambda0, Lambda, q_row_sum, p, p0, P_theta, q, logp, theta, Y, Delta, X, Bspline_uni, ZW, X_uni, X_uni_ind, Bspline_uni_ind, Y_uni_event, Y_uni_event_n, Y_risk_ind, p_static, n, n2, m, n_event_uni, s, n_minus_n2, X_nc, ZW_nc, MAX_ITER, TOL)
}

TwoPhase_GeneralSpline_coxph <- function(Y, Delta, X, ZW, Bspline, hn, MAX_ITER, TOL, noSE) {
    .Call(`_CombinedReg_TwoPhase_GeneralSpline_coxph`, Y, Delta, X, ZW, Bspline, hn, MAX_ITER, TOL, noSE)
}

TwoPhase_MLE0_coxph <- function(Y, Delta, X, ZW, MAX_ITER, TOL, noSE) {
    .Call(`_CombinedReg_TwoPhase_MLE0_coxph`, Y, Delta, X, ZW, MAX_ITER, TOL, noSE)
}

TwoPhase_MLE0_noZW_coxph <- function(Y, Delta, X, MAX_ITER, TOL, noSE) {
    .Call(`_CombinedReg_TwoPhase_MLE0_noZW_coxph`, Y, Delta, X, MAX_ITER, TOL, noSE)
}

matTimesVec <- function(mat, v) {
    .Call(`_CombinedReg_matTimesVec`, mat, v)
}

matDivideVec <- function(mat, v) {
    .Call(`_CombinedReg_matDivideVec`, mat, v)
}

lengthenWT <- function(w_t_original, n, modifyW_T = TRUE) {
    .Call(`_CombinedReg_lengthenWT`, w_t_original, n, modifyW_T)
}

calculateMu <- function(design_mat, prev) {
    .Call(`_CombinedReg_calculateMu`, design_mat, prev)
}

calculateGradient <- function(w_t, n, design_mat, Y_col, muVector, modifyW_T = FALSE) {
    .Call(`_CombinedReg_calculateGradient`, w_t, n, design_mat, Y_col, muVector, modifyW_T)
}

calculateHessian <- function(design_mat, w_t, muVector, n, mus, modifyW_T = FALSE) {
    .Call(`_CombinedReg_calculateHessian`, design_mat, w_t, muVector, n, mus, modifyW_T)
}

pYstarCalc <- function(gamma_design_mat, n, excludeRows, prev_gamma, comp_dat_all, Y_unval_index, pYstar, mu_gamma) {
    .Call(`_CombinedReg_pYstarCalc`, gamma_design_mat, n, excludeRows, prev_gamma, comp_dat_all, Y_unval_index, pYstar, mu_gamma)
}

pXCalc <- function(n, comp_dat_all_cropped, errorsX, errorsY, pX, prevRows) {
    .Call(`_CombinedReg_pXCalc`, n, comp_dat_all_cropped, errorsX, errorsY, pX, prevRows)
}

TwoPhase_GeneralSpline <- function(Y, X, ZW, Bspline, hn, MAX_ITER, TOL, noSE) {
    .Call(`_CombinedReg_TwoPhase_GeneralSpline`, Y, X, ZW, Bspline, hn, MAX_ITER, TOL, noSE)
}

TwoPhase_MLE0 <- function(Y, X, ZW, MAX_ITER, TOL, noSE) {
    .Call(`_CombinedReg_TwoPhase_MLE0`, Y, X, ZW, MAX_ITER, TOL, noSE)
}

TwoPhase_MLE0_MEXY <- function(Y_tilde, X_tilde, Y, X, Z, Bspline, hn, MAX_ITER, TOL, noSE) {
    .Call(`_CombinedReg_TwoPhase_MLE0_MEXY`, Y_tilde, X_tilde, Y, X, Z, Bspline, hn, MAX_ITER, TOL, noSE)
}

LMM_GeneralSpline <- function(Y, T, X, Z, ZT_colid, Bspline, index_obs, coef_initial, vc_initial, hn, MAX_ITER, TOL, noSE) {
    .Call(`_CombinedReg_LMM_GeneralSpline`, Y, T, X, Z, ZT_colid, Bspline, index_obs, coef_initial, vc_initial, hn, MAX_ITER, TOL, noSE)
}

TwoPhase_GeneralSpline_logistic <- function(Y, X, ZW, Bspline, hn, MAX_ITER, TOL, noSE) {
    .Call(`_CombinedReg_TwoPhase_GeneralSpline_logistic`, Y, X, ZW, Bspline, hn, MAX_ITER, TOL, noSE)
}

TwoPhase_MLE0_logistic <- function(Y, X, ZW, MAX_ITER, TOL, noSE) {
    .Call(`_CombinedReg_TwoPhase_MLE0_logistic`, Y, X, ZW, MAX_ITER, TOL, noSE)
}

